on pause, switch the seed to a more "subtle", more faded color from the current seed to make the ui feels "faded back". Then, on resume, go back to the normal color seed.

An exception, when clicking on artist/album page, if it has custom color seed set, use that seed for that page (whether the surroundings UI like player, tabbars,etc. should follow too is up to debate).