/** ISR revalidation window for public catalog pages (seconds). */
export const PUBLIC_PAGE_REVALIDATE_SECONDS = 3600;

/** Sitemap regeneration interval (seconds). */
export const SITEMAP_REVALIDATE_SECONDS = 86400;
